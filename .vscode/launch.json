{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "(lldb) Launch target/a.out",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/target/a.out",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "externalConsole": false,
      "MIMode": "lldb",
      "preLaunchTask": "Build C++ All"
    }
  ]
}
